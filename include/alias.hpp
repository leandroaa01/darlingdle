
#include <string>
#include <vector>
#include <fstream>
#include <sstream>
#include <iostream>
#include <random>
#include <algorithm>



#define print(x) std::cout << x << "\n";
#define error(x) std::cerr << x << "\n";
#define  read(x) std::getline(std::cin, x);

template <typename type> using vec = std::vector<type>;
using str = std::string;
using strtream =  std::stringstream;
using flag = bool;
using size_type = std::size_t;